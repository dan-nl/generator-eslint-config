'use strict';

/**
 * @type {string}
 */
var intro = [
  '',
  'this generator will run the following tasks:',
  '',
  '  * install eslint and an eslint shared config',
  ''
].join( '\n' );

module.exports = intro;
